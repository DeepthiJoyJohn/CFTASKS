function novalidation(form, ctrl, value) {

				if (isNaN(value) || value=="") 
				{
					return (false);
				} 
				else
				{
					return (true);
				} 
}
function valuenull(form, ctrl, value) {


				if (value=="") 
				{
					return (false);
				} 
				else
				{
					return (true);
				} 
}
